<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:defaultValue=""
        android:entries="@array/pref_image_size_titles"
        android:entryValues="@array/pref_image_size_values"
        android:key="image_size"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_image_size" />

    <ListPreference
        android:defaultValue=""
        android:entries="@array/pref_color_filter_titles"
        android:entryValues="@array/pref_color_filter_values"
        android:key="color_filter"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_color_filter" />

    <ListPreference
        android:defaultValue=""
        android:entries="@array/pref_image_type_titles"
        android:entryValues="@array/pref_image_type_values"
        android:key="image_type"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_image_type" />

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->
    <EditTextPreference
        android:capitalize="words"
        android:defaultValue="@string/pref_default_site_filter"
        android:inputType="textCapWords"
        android:key="site_filter"
        android:maxLines="1"
        android:selectAllOnFocus="true"
        android:singleLine="true"
        android:title="@string/pref_title_site_filter" />

</PreferenceScreen>
